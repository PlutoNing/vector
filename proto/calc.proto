syntax = "proto3";

package calc;

service Calculator {
    rpc Add (AddRequest) returns (AddResponse);
}

message AddRequest {
    int32 val1 = 1;
    int32 val2 = 2;
}

message AddResponse {
    int32 result = 1;
}
